services:
  node-react-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: pareta22/node-react-demo:latest
    container_name: node-react-demo
    ports:
      - "3001:3000"
    restart: unless-stopped
